/// Useful for running a given day's code, outside of the test suite.
fn main() {
    // let input = aoc::get_input("2023", "05", None);
    // let output = aoc::year_2023::day_05::part_2(&input);
    // println!("{}", output);
}
